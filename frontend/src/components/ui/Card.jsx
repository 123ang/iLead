export function Card({title, children, className=''}){return <section className={`rounded-2xl border border-slate-200 bg-white p-5 shadow-sm ${className}`}>{title&&<h2 className="mb-3 font-semibold text-slate-800">{title}</h2>}{children}</section>}
